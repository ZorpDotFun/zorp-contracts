# zorp-contracts

Zorp launchpad contracts for Arc (chain 5042). Foundry, Solidity 0.8.26, viaIR, Cancun.

```bash
forge install
forge test
```

Deploy with `script/Deploy.s.sol`. Copy `.env.example` to `.env` — never commit keys.
